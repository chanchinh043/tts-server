# Python
venv/
__pycache__/
*.pyc

# Config / secrets
.env
credentials/

# DB
*.db

# Audio data — chặn theo PHẦN MỞ RỘNG (an toàn hơn chỉ chặn theo thư mục,
# vì file audio có thể lỡ tay tạo/test ở bất kỳ đâu trong repo)
*.wav
*.ogg
*.opus
*.mp3

# Server working dirs (config.py)
tts_output/
tts_zips/
tts_wav_tmp/

# step3_package_zip.py chạy độc lập (dòng lệnh) — thư mục/state riêng
packaged_audio/
zips/
version_state.json
pending_upload.json

# Editor
.vscode/
.idea/

# bỏ qua thư mục generate thủ công
generated_audio/
db/